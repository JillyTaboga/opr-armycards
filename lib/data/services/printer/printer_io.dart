import 'dart:typed_data';

Future<void> printImages(
  List<Uint8List> imagesBytes,
  double paperWidth,
  double cardWidth,
  double cardHeight,
) async {
  // Safe no-op for desktop native builds
}
